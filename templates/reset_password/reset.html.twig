{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="font-sizerequest">Réinitialiser votre mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary requestform">Réinitialiser le mot de passe</button>
    {{ form_end(resetForm) }}
    </div>
{% endblock %}
